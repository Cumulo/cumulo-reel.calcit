# 202608292005 Upgrade Calcit and collaboration dependencies / 升级 Calcit 与协同依赖

- Upgraded Calcit and `@calcit/procs` together to 0.13.63 and aligned released Respo, Recollect, WebSocket, standard-library, and UI dependencies.
- 将 Calcit 与 `@calcit/procs` 同步升级到 0.13.63，并对齐已发布的 Respo、Recollect、WebSocket、标准库与 UI 依赖。
- Replaced the opaque setup action commit with `calcit-lang/setup-calcit@v1` and explicit `calcit,caps` tools; replaced the server IR debug check with real server JavaScript codegen.
- 将不透明的 setup action commit 改为 `calcit-lang/setup-calcit@v1` 并显式安装 `calcit,caps`；以真实服务端 JavaScript codegen 替代 IR 调试检查。
- Canonically formatted the current Snapshot and made both browser event callbacks return `Unit` explicitly under the stricter toolchain.
- 使用当前工具链规范化 Snapshot，并让两个浏览器事件 callback 显式返回 `Unit`。
- Client/server checks, one definition test, deprecated analysis, client/server codegen, and the Node 24/Vite production build pass.
- client/server 检查、1 项定义测试、弃用分析、双 entry codegen 与 Node 24/Vite 生产构建均通过。
