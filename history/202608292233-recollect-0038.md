# Upgrade recollect to 0.0.38

- Propagate namespace-qualified validated patch schemas to downstream applications.
- Keep the strict dependency graph on one stable recollect release.
- Allow calcium-workflow to specialize `PatchBatch.apply-to` across the module boundary.
