# Finalize strict dependency alignment

- Upgrade respo-message to 0.0.15, respo-ui to 0.7.12, and recollect to 0.0.36.
- Enforce warning-free strict dependency resolution in CI.
- Pin `@calcit/procs` exactly at the `deps.cirru` Calcit version to prevent toolchain drift.
