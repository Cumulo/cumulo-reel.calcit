# Align ws-edn 0.0.19 / 对齐 ws-edn 0.0.19

## 中文

- 将 Calcit CLI 与 `@calcit/procs` 精确同步到 0.13.66。
- 将 `mvc-works/ws-edn.calcit` 稳定 tag 升级到 0.0.19，使下游 Calcium
  Workflow 可以在 strict dependency graph 中使用 generation-gated client。
- 不改变 cumulo-reel 的协议和运行逻辑；本次改动只清理直接依赖 pin，并通过
  client/server 检查、完整 strict graph 与双 entry JS 构建验证兼容性。

## English

- Align the exact Calcit CLI / `@calcit/procs` pair at 0.13.66.
- Upgrade the stable `mvc-works/ws-edn.calcit` tag to 0.0.19 so downstream
  Calcium Workflow can consume the generation-gated client in a strict graph.
- Keep cumulo-reel protocol/runtime behavior unchanged; this update only
  removes the stale direct pin and validates compatibility with client/server
  checks, the complete strict graph, and both JavaScript entries.
