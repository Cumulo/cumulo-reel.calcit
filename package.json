{
  "dependencies": {
    "@calcit/procs": "^0.3.0-a11"
  },
  "scripts": {
    "watch-server": "calcit_runner --emit-js --init-fn cumulo-reel.server/main! --reload-fn cumulo-reel.server/main!",
    "build-server": "target=node entry=./server.js yarn webpack && node js-out/bundle.js",
    "compile-server": "calcit_runner --emit-js --once --init-fn cumulo-reel.server/main! --reload-fn cumulo-reel.server/main!",
    "compile-page": "calcit_runner --emit-js --once",
    "watch-page": "calcit_runner --emit-js --emit-path out-page/"
  },
  "devDependencies": {
    "md5": "^2.3.0",
    "randomcolor": "^0.6.2",
    "shortid": "^2.2.16",
    "vite": "^2.2.3",
    "webpack": "^5.36.0",
    "webpack-cli": "^4.6.0",
    "ws": "^7.4.5"
  },
  "version": "0.0.2"
}
